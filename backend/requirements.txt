# Core Framework
#
# Plain fastapi, not fastapi[all]. The [all] extra installs a dozen packages,
# and every one this project actually uses is pinned explicitly further down:
# uvicorn, python-multipart, itsdangerous, email-validator, pydantic,
# websockets, httpx, python-dotenv. The only thing it contributed that was not
# pinned was Jinja2, which is now.
#
# What it also installs is fastapi-cli and, since 0.140, fastapi-cloud-cli: a
# deployment client for FastAPI's hosted service, in the image of a product
# whose argument is that you host it yourself. It is also what made the
# upgrade look impossible. pip could not resolve fastapi-cloud-cli against the
# rest, backtracked through every version of an unrelated package, and ended
# up at a 2010 release of ecdsa whose setup.py uses print as a statement. The
# error named ecdsa and had nothing to do with it.
fastapi==0.141.1
jinja2==3.1.6
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# Database
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.19.2

# Security & Authentication
itsdangerous==2.2.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4

# Email
aiosmtplib==5.1.2
email-validator==2.3.0

# Payments

# Settings & Configuration
python-dotenv==1.2.3
pydantic==2.13.5
pydantic-settings==2.15.0

# Utilities
python-dateutil==2.9.0.post0
user-agents==2.2.0

# AI & Chatbot

# GeoIP
geoip2==4.7.0

# Scheduling
apscheduler==3.11.3

# WebSocket (included in fastapi[all])
websockets==17.1

# Development & Testing
pytest==7.4.4
httpx==0.26.0

# Production Server
gunicorn==26.2.0
